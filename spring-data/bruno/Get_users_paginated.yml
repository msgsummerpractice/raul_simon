info:
  name: Get_users_paginated
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
