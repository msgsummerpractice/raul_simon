info:
  name: delete_user
  type: http
  seq: 12

http:
  method: DELETE
  url: http://localhost:8080/delete/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
