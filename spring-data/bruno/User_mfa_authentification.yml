info:
  name: User_mfa_authentification
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/api/auth/mfa
  body:
    type: json
    data: |-
      {
        "username": "john11",
        "mfaCode": "039423"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
